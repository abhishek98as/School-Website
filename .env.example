# Example environment variables for local/dev and Netlify
# Copy this file to `.env.local` for local dev, and add the same keys in Netlify UI

# Next.js
NEXT_TELEMETRY_DISABLED=1

# Genkit / Google AI
# Choose a default model (optional; falls back to googleai/gemini-2.5-flash)
GENKIT_MODEL=googleai/gemini-2.5-flash

# If you add API keys later, document them here, e.g.:
# GOOGLE_API_KEY=
# FIREBASE_API_KEY=
# NEXT_PUBLIC_ANALYTICS_ID=
